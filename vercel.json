{
    "version": 2,
    "builds": [
      {
        "src": "docs/*",
        "use": "@vercel/static"
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/docs/$1" }
    ]
}